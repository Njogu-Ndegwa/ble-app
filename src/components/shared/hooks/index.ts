/**
 * Shared Hooks
 * 
 * Custom hooks for common functionality across workflows
 */

// useBleScanner (legacy) is deprecated and NOT exported - use the hooks in
// src/lib/hooks/ble, which carry the self-healing and shared progress UX.
